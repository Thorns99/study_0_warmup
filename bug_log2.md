今日bug记录 日期：阶段0 day02 练习内容：元组、集合、函数，容器综合练习 

现象1：定义单元素元组写 t=(5)，当成元组使用，实际识别成数字int类型。
原因：单元素元组必须加逗号，`(5)`只是括号包裹数字，不是元组。
解决：改成 t=(5,)。

现象2：调用函数时，用print打印函数内结果，没有写return，拿不到返回值，后续无法用这个结果做计算。
原因:print只是把内容输出在控制台；return才是把数据返回给调用方，两个作用不一样。
解决:函数内部使用return返回结果，需要查看时再调用print(函数())。

现象3：分不清局部变量和全局变量，函数内部修改同名变量，外面全局变量值不变，以为代码出错。
原因：函数内直接赋值同名变量，会新建局部变量，不会修改外部全局变量。
解决：不加global关键字时，函数内部的变量只在函数里生效。如需修改全局变量，要声明global。

现象4：集合创建时使用s={}，以为是空集合，实际创建出来是空字典。
原因：`{}`默认代表字典；空集合必须用 set() 创建。
解决：empty_set = set()